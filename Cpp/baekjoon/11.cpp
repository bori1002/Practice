#include <iostream>
#include <vector>

using namespace std;

int main(){
    int t = 0;
    vector<vector<int>> nums;
    cin >> t;
    for(int i = 0; i  < t; i++){
        cin >> a >> b;
        nums[i].push_back(a);
        nums[i].push_back(b);
    }
    
}